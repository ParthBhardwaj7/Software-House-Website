/**
 * Default wordmark served from /public. Override in Admin → Logo image URL.
 *
 * Suggested source asset (for sharp header + hero):
 * - Horizontal wordmark ~4:1 aspect, e.g. 480×120px (960×240 @2x).
 * - Safe zone: keep important shapes inside ~90% of the canvas.
 * - Formats: SVG preferred; else PNG with transparent background.
 */
export const DEFAULT_SITE_LOGO_PATH = "/apn-codix-logo.svg";
